# Phase 0/1 only needs the simulation stack. FastAPI and uvicorn arrive in Phase 5
# when app.py gets real routes; installing them now would be dead weight.
numpy==2.1.3
networkx==3.4.2
pytest==8.3.4
